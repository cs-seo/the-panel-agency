"use client";

// Temporarily stripped while debugging Netlify build.
// Will be reintroduced once the failure is identified and fixed.
export type LeadFormName = "newsletter" | "lead-magnet" | "consult-request";
export function LeadCaptureForm() {
  return null;
}
